[Unit]
Description=SkyPro parser
After=network.target

[Service]
WorkingDirectory=/home/sky_parser
ExecStart=echo $SERVER_PASSWORD | sudo -S docker-compose up -d --build

[Install]
WantedBy=multi-user.target
